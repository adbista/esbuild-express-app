import type { LogRecordProcessor } from '@opentelemetry/sdk-logs';
import type { Resource } from '@opentelemetry/resources';
import type { ResourceFactory } from '../types';
export type LogRecordProcessorFactory = (options: LoggingOptions) => LogRecordProcessor | LogRecordProcessor[];
export interface LoggingOptions {
    accessToken?: string;
    realm?: string;
    serviceName: string;
    endpoint?: string;
    resource: Resource;
    logRecordProcessorFactory: LogRecordProcessorFactory;
}
export type StartLoggingOptions = Partial<Omit<LoggingOptions, 'resource'>> & {
    resourceFactory?: ResourceFactory;
};
//# sourceMappingURL=types.d.ts.map